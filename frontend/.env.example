# Public endpoints (optional; if unset, app falls back to local /api/mcp/* routes)
NEXT_PUBLIC_MCP_PLAN=
NEXT_PUBLIC_MCP_GENERATE=
NEXT_PUBLIC_MCP_REGENERATE=

# Email (optional) for Success page send-confirmation API
RESEND_API_KEY=
EMAIL_FROM=
