# Frontend environment example for DynoTrip
# Copy to .env.local and fill values locally. Do NOT commit your real .env.local.

# Point UI to real backend (optional). If not set, UI uses local mock API routes.
# Example: http://localhost:8000/travel-stay
NEXT_PUBLIC_MCP_PLAN=
